export const report = (fetch = {}, params = {}) => {
    return fetch.then(function(response) {
        return response;
    }, function(response) {
        return response;
    });
};

export default report;
